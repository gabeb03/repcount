mutation SignIn($email: String!, $password: String!) {
  signInUser(email: $email, password: $password) {
    success
    errors {
      message
      code
    }
    user {
      id
      email
    }
  }
}
